using System.Numerics;

namespace RayTracer
{
    class Camera
    {
        // Camera position
        public Vector3 pos = new Vector3(0, 0, 0);

        // Camera direction
        public Vector3 d = new Vector3(0, 0, 1);

    }
}
